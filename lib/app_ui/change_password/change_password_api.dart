import 'dart:convert';

import 'package:http/http.dart' as http;


/*
Future<MessageResponse> getChangePasswordStatus(Map<String, String> header, Map<String, String> body) async {

  var response = await http.post(Uri.parse(baseUrl + changePassword),
      headers : header,
      body: body);
  MessageResponse apiResponse =
  MessageResponse.fromJson(jsonDecode(response.body));
  return apiResponse;
*/

